"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from) {
    for (var i = 0, il = from.length, j = to.length; i < il; i++, j++)
        to[j] = from[i];
    return to;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.arrayStringSlice = void 0;
var ts = require("typescript");
var _propName_1 = require("./_propName");
var basicTypes_1 = require("../../components/typeInference/basicTypes");
var ast_1 = require("../../utils/ast");
var renderNodes_1 = require("../../components/codegen/renderNodes");
function isForcedArrayType(context, node) {
    var _a;
    var left = (_a = ast_1.getLeftExpr(node.expression)) === null || _a === void 0 ? void 0 : _a.getText();
    if (!left) {
        return false;
    }
    var _b = __read(context.scope.findByIdent(left) || [], 1), decl = _b[0];
    if (!decl) {
        return false;
    }
    return decl.forcedType === 'array';
}
/**
 * String.prototype.slice support
 * Array.prototype.slice support
 *
 * @param node
 * @param context
 */
var arrayStringSlice = function (node, context) {
    if (!_propName_1.propNameIs('slice', node)) {
        return undefined;
    }
    var nd = node.expression.expression;
    var type = context.checker.getTypeAtLocation(nd);
    var varNameNode = ast_1.getCallExpressionLeftSide(node);
    if (type.isStringLiteral() || context.checker.typeToString(type, nd, ts.TypeFormatFlags.None) === 'string') {
        var args = renderNodes_1.renderNodes(__spreadArray([], __read(node.arguments)), context);
        var varName = renderNodes_1.renderNode(varNameNode, context);
        if (!args || !args[0]) {
            return varName;
        }
        context.log.warn('Converting String.prototype.substr to substr(): check your encodings twice!', [], context.log.ctx(node));
        if (args[1]) {
            return "Stdlib::stringSlice(" + varName + ", " + args[0] + ", " + args[1] + ")";
        }
        else {
            return "Stdlib::stringSlice(" + varName + ", " + args[0] + ")";
        }
    }
    else {
        var forced = isForcedArrayType(context, node);
        if (!basicTypes_1.hasArrayType(node.expression, context.checker, context.log) && !forced) {
            context.log.error('Left-hand expression must have string, array-like or iterable inferred type', [], context.log.ctx(node));
            return 'null';
        }
        if (forced) {
            ast_1.flagParentOfType(node, [ts.SyntaxKind.VariableDeclarationList], { forceType: 'split' }, context.nodeFlagsStore);
        }
        var args = renderNodes_1.renderNodes(__spreadArray([], __read(node.arguments)), context);
        var varName = renderNodes_1.renderNode(varNameNode, context);
        if (!args || !args[0]) {
            return "Stdlib::arraySlice(" + varName + ", 0)";
        }
        if (args[1]) {
            return "Stdlib::arraySlice(" + varName + ", " + args[0] + ", " + args[1] + ")";
        }
        else {
            return "Stdlib::arraySlice(" + varName + ", " + args[0] + ")";
        }
    }
};
exports.arrayStringSlice = arrayStringSlice;
