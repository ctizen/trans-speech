"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.objectValues = void 0;
var ts = require("typescript");
var ast_1 = require("../../utils/ast");
var renderNodes_1 = require("../../components/codegen/renderNodes");
/**
 * Object.values support
 *
 * @param node
 * @param context
 */
var objectValues = function (node, context) {
    var toCheck = node.expression.kind === ts.SyntaxKind.PropertyAccessExpression &&
        node.expression.expression.getText() === 'Object' &&
        node.expression.name.escapedText === 'values';
    if (toCheck) {
        var varName = renderNodes_1.renderNode(ast_1.getCallExpressionArg(node), context);
        return "array_values(" + varName + ")";
    }
};
exports.objectValues = objectValues;
