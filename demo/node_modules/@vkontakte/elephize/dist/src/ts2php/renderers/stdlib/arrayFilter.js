"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.arrayFilter = void 0;
var _propName_1 = require("./_propName");
var basicTypes_1 = require("../../components/typeInference/basicTypes");
var ast_1 = require("../../utils/ast");
var renderNodes_1 = require("../../components/codegen/renderNodes");
/**
 * Array.prototype.filter support
 *
 * @param node
 * @param context
 */
var arrayFilter = function (node, context) {
    if (!_propName_1.propNameIs('filter', node)) {
        return undefined;
    }
    if (!basicTypes_1.hasArrayType(node.expression, context.checker, context.log)) {
        context.log.error('Left-hand expression must have array-like or iterable inferred type', [], context.log.ctx(node));
        return 'null';
    }
    var funcNode = ast_1.getCallExpressionCallbackArg(node);
    var varNode = ast_1.getCallExpressionLeftSide(node);
    if (((funcNode === null || funcNode === void 0 ? void 0 : funcNode.parameters.length) || 0) > 1) {
        context.log.error('Array.prototype.filter with index in callback is not supported', [], context.log.ctx(node));
        return 'null';
    }
    var _a = __read(renderNodes_1.renderNodes([funcNode, varNode], context), 2), renderedFunction = _a[0], varName = _a[1];
    return "array_filter(" + varName + ", " + renderedFunction + ")";
};
exports.arrayFilter = arrayFilter;
